# AI 作業ガイド

このリポジトリでコードや文書を生成・変更する AI は、作業開始時にこのファイルを確認します。

## 読む順序

1. タスクの開始時に一度だけ `git status --short` を実行し、無関係な変更を触らない。
2. 仕様や配置の判断が必要な場合だけ [Documentation/INDEX.md](Documentation/INDEX.md) から該当文書を選ぶ。
3. ファイルの追加・移動は [Documentation/FOLDER_RULES.md](Documentation/FOLDER_RULES.md)、UI の意味・構造・操作変更は [Documentation/LAYOUT_CONCEPT.md](Documentation/LAYOUT_CONCEPT.md) を参照する。
4. 振る舞いを変えるコード変更は [Documentation/TEST_STRATEGY.md](Documentation/TEST_STRATEGY.md) に従う。

同じタスク内で、変更されていない規約文書を読み直しません。単純な質問、調査、文言・文書だけの変更では、関係しない仕様書を読みません。

## 基本方針

- 現在の小規模な構成を優先し、将来のためだけの階層や抽象化を作らない。
- UI、タスク操作、Tauri 起動、DB マイグレーションの責務境界を越えて処理を混在させない。
- 既存の日本語表示、ローカルファースト、ダークテーマ、キーボード操作を維持する。
- 仕様と実装が食い違う場合は、黙って一方に合わせず差異を報告する。
- 変更リスクに合う最小の品質ゲート（Q0〜Q3）を選び、未実施の目視・実機確認を成功扱いしない。
- 新機能、不具合修正、計算・データ操作の変更には、回帰を検出できる自動テストを追加・更新する。文書、コメント、表示文言、振る舞いを変えないスタイル調整だけならテスト追加は不要とする。
- CUIテストは外部から利用する重要な振る舞い、CTは複数条件の組み合わせに意味がある処理へ適用する。公開メソッドや条件分岐の存在だけを理由に一律追加しない。
- `npm run verify` は Q2・Q3 と、統合確認を依頼された場合に実行する。Q0・Q1 は対象に応じた軽量な確認でよい。

## 完了報告

変更内容、利用者への影響、実行した検証、重要な未確認事項だけを簡潔に報告します。影響がない項目は列挙しません。コミットや大規模な整形は、依頼に含まれる場合だけ行います。
